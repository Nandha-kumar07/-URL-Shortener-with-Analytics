{
  "name": "trimly-url-shortener",
  "version": "1.0.0",
  "description": "Fullstack URL Shortener with Analytics built using React, Express, and Supabase",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/index.js"
  }
}
